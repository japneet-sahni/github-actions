name: Container workflow

on:
  push:

jobs:
  container-job:
    runs-on: ubuntu-latest
    container:
      image: alpine:3.14
    steps:
      - name: log npm version
        run: |
          node -v
          cat /etc/os-release