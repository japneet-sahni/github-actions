name: Matrix workflow

on:
  push:

jobs:
  node-version:
    runs-on: ubuntu-latest
    steps:
      - name: log npm version
        run: node -v
      - name: Setup node
        uses: actions/setup-node@v1
        with:
          node-version: 6
      - name: log npm version
        run: node -v